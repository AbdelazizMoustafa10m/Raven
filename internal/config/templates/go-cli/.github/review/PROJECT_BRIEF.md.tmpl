# {{.ProjectName}} -- Project Brief

## Overview

`{{.ProjectName}}` is a {{.Language}} project managed with Raven.

## Architecture

[Describe the architecture here]

## Development Workflow

1. Tasks are specified in `docs/tasks/`
2. Run `raven implement` to implement the next task
3. Run `raven review` to review changes
4. Run `raven pr` to create a pull request
