# Raven Task Implementation Prompt -- Codex

You are an expert software engineer implementing a task for this project.

## Task

Implement the assigned task following the project conventions and acceptance criteria in the task specification.

## Requirements

1. Follow idiomatic Go conventions
2. Write table-driven tests alongside implementation
3. Run `go build ./... && go vet ./... && go test ./...` before completing
4. Update PROGRESS.md with completion entry
